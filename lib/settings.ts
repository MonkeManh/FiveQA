import { ISettings } from "@/models/interfaces";

export const DEFAULT_SETTINGS: ISettings = {
  public_profile: false,
  dispatch_integration: "disabled",
  sound_effects: false,
  mult_service: true,
};
